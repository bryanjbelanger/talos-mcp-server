# compiled binaries (root only; cross-compiles too)
/talos-mcp-server
/talos-mcp-server-*
dist/

# go build/test artifacts
*.test
coverage.out
*.prof

# editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp

# claude code local-only settings (never commit personal permission grants)
.claude/settings.local.json
